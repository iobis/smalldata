{
  "name": "smalldata",
  "version": "1.0.0",
  "private": true,
  "homepage": "/occurrences",
  "repository": "https://github.com/iobis/smalldata",
  "license": "MIT",
  "main": "index.js",
  "workspaces": [
    "admin-app",
    "dwca-lib",
    "occurrences-app"
  ],
  "scripts": {
    "admin-app:build": "yarn workspace @smalldata/admin-app build",
    "admin-app:copy-to-server": "shx mkdir -p ./server/src/main/resources/webroot/ && shx cp -rf ./admin-app/build/* ./server/src/main/resources/webroot/",
    "admin-app:start": "yarn workspace @smalldata/admin-app start",
    "build:back-end": "run-s server:bundle-openapi-spec server:runtime-zip",
    "build:front-end": "run-s occurrences-app:build occurrences-app:copy-to-server",
    "build:zip": "run-s build:front-end build:back-end",
    "clean": "shx rm -rf ./occurrences-app/build ./server/src/main/resources/webroot/*",
    "lint": "yarn workspaces run lint",
    "occurrences-app:build": "yarn workspace @smalldata/occurrences-app build",
    "occurrences-app:copy-to-server": "shx mkdir -p ./server/src/main/resources/webroot/ && shx cp -rf ./occurrences-app/build/* ./server/src/main/resources/webroot/",
    "occurrences-app:start": "yarn workspace @smalldata/occurrences-app start",
    "prettier": "prettier --print-width 100 --jsx-bracket-same-line --single-quote --no-semi --write **/*.{scss,css,json} !server/**/resources/**/*",
    "server:bundle-openapi-spec": "swagger-cli bundle server/src/main/resources/swaggerroot/spec/smalldata.yaml -t yaml -o server/src/main/resources/swaggerroot/spec/smalldata-bundled.yaml",
    "server:runtime-zip": "cd server && ./gradlew runtimeZip",
    "server:start": "cd server && ./gradlew run",
    "sort-package-json": "sort-package-json package.json admin-app/package.json dwca-lib/package.json occurrences-app/package.json",
    "start": "run-p server:start admin-app:start occurrences-app:start",
    "test:ci": "yarn test:front-end:ci && yarn test:server:ci",
    "test:front-end:ci": "yarn workspaces run test:ci",
    "test:server:ci": "cd server && ./gradlew check --no-daemon"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "prettier": "1.18.2",
    "shx": "0.3.2",
    "sort-package-json": "1.22.1",
    "swagger-cli": "2.2.1"
  }
}
